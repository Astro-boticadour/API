meta {
  name: GetAdminToken
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/login
  body: none
  auth: basic
}

auth:basic {
  username: admin
  password: admin
}

script:post-response {
  let body = res.getBody();
  bru.setVar("token",body.result.token);
}
